# Auto Audit — 2026-05-09T20:13:24Z

## Part 1 — Public Surface Health

| URL | Expected | Got | Status |
|-----|----------|-----|--------|
| `https://www.thegoatnote.com/4UWHAt/` | 308 or 200 | 403 | **FAIL** |
| `https://www.thegoatnote.com/` | 200 | 403 | **FAIL** |

> Both endpoints returned 403 — site may be down or Vercel deployment is blocked.

---

## Part 2 — Best-Practices Checklist

| # | Item | Result | Evidence |
|---|------|--------|----------|
| 1 | CLAUDE.md with §0 hot-path markers | **PASS** | `CLAUDE.md` present at root; §0 section confirmed with RunPod PTY and secret-grep guard docs |
| 2 | `.claude/skills/` has procedural-knowledge skills | **FAIL** | Directory does not exist |
| 3 | `.claude/agents/` has subagent definitions | **FAIL** | Directory does not exist |
| 4 | `.claude/settings.json` has hooks (UserPromptSubmit, PreToolUse, etc.) | **FAIL** | File does not exist; hook scripts exist only under `scripts/hooks/` (not wired to Claude Code) |
| 5 | `.mcp.json` for external tools | **FAIL** | File does not exist |
| 6 | `tests/` with discoverable pytest tests | **PASS** | 8 test files found (`test_clinical_demo_artifacts.py`, `test_triton_judge.py`, `test_healthbench_judge.py`, etc.) |
| 7 | `scripts/` automation (no ask-user-to-run pattern) | **PASS** | 50+ scripts present covering serve, bench, eval, build, and health-check workflows |
| 8 | `.env` never `cat`-ed in scripts | **PASS** | `grep -rE 'cat \.env'` returns 0 matches |
| 9 | `.pre-commit-config.yaml` present | **PASS** | File exists at repo root |
| 10 | `PROGRAM.md` charter with `allow_paths`/`lock_paths` | **FAIL** | No `PROGRAM.md` found; frozen paths documented in CLAUDE.md §7 but no machine-readable charter |
| 11 | Single canonical metric for active eval/RL loop | **PARTIAL** | `findings/research/2026-04-29-omni-canonical/CANONICAL.md` exists but no single top-level metric constant referenced by eval scripts; ambiguity between HealthBench-hard, MedQA, and reward scores |
| 12 | `EXPERIMENT_LOG.jsonl` append-only ledger | **FAIL** | No `EXPERIMENT_LOG*` file found anywhere in repo |
| 13 | Eval/judge code locked from agent-edit | **PARTIAL** | CLAUDE.md §7 locks `data/seed_kg/` and `corpus/pins/` but does not explicitly lock `scripts/healthbench_runner.py`, `scripts/ship_rule_eval.py`, or judge-serving scripts; no `lock_paths` in a PROGRAM.md |

---

## Part 3 — Top 3 Recommended Next Actions

1. **Create `.claude/settings.json` with Claude Code hooks** — Items 2, 3, and 4 all fail because the entire `.claude/` config layer is absent. Adding `UserPromptSubmit` and `PreToolUse` hooks here (wiring the existing `scripts/hooks/no_cloud_llm_keys.sh` and `no_prod_url_leak.sh`) closes three checklist gaps at once and activates the guardrails that are already written but currently unexecuted.

2. **Add `findings/PROGRAM.md` with machine-readable `allow_paths` / `lock_paths`** — Required for a safe autoresearch loop. Should explicitly lock `scripts/healthbench_runner.py`, `scripts/ship_rule_eval.py`, `scripts/grade_hb_parallel.py`, and the judge-serving scripts from agent edits, and declare the single canonical eval metric (recommendation: `healthbench_hard_1000_reward_mean`).

3. **Create `EXPERIMENT_LOG.jsonl`** — An append-only JSONL ledger (one entry per eval run: timestamp, model tag, metric values, artifact path) is the lowest-overhead way to establish reproducibility lineage and satisfy the Karpathy autoresearch pattern. The `scripts/write_card.py` workflow could emit to it automatically.

---

*Generated by Claude Code hourly audit loop — read-only pass, no non-audit files modified.*
