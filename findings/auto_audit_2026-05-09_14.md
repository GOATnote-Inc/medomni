# Auto Audit — 2026-05-09T14:14:07Z

## PART 1 — Public Surface Health

| URL | Expected | Got | Status |
|-----|----------|-----|--------|
| `https://www.thegoatnote.com/4UWHAt/` | 308 or 200 | **403** | ❌ FAIL |
| `https://www.thegoatnote.com/` | 200 | **403** | ❌ FAIL |

**Both endpoints returned 403 — site appears down or access-restricted.**

---

## PART 2 — Best-Practices Checklist

### Claude Code

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | CLAUDE.md in repo root with §0 hot-path markers | PASS | `CLAUDE.md` present, §0 section with RunPod PTY warning and token-leak mitigations |
| 2 | `.claude/skills/` has procedural-knowledge skills | FAIL | `.claude/` directory does not exist |
| 3 | `.claude/agents/` has subagent definitions | FAIL | `.claude/` directory does not exist |
| 4 | `.claude/settings.json` has hooks (UserPromptSubmit, PreToolUse, etc.) | FAIL | `.claude/` directory does not exist |
| 5 | `.mcp.json` present (if external tools used) | FAIL | `.mcp.json` not found; no MCP server configuration |
| 6 | `tests/` exists with pytest tests discoverable | PASS | 8 test files: `test_clinical_case.py`, `test_healthbench_judge.py`, `test_ship_rule_eval.py`, etc. |

### Boris Cherny 'no terminal' pattern

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 7 | `scripts/` has automation scripts | PASS | 50+ scripts including `preflight.sh`, `run_demo.sh`, `sovereign_bench.py`, etc. |
| 8 | `.env` handling never uses `cat .env` | PASS | `grep -rE 'cat \.env' scripts/` → 0 matches |
| 9 | Pre-commit hooks for safety | PASS | `.pre-commit-config.yaml` present; `scripts/hooks/` has `no_cloud_llm_keys.sh` and `no_prod_url_leak.sh` |

### Karpathy autoresearch

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 10 | `PROGRAM.md` (or similar charter) under `findings/` with allow_paths/lock_paths | FAIL | No `PROGRAM.md` or `CHARTER.md` found anywhere under `findings/` |
| 11 | Single canonical metric defined for active eval/RL loop | PARTIAL | CARD.md defines gpt-4.1-2025-04-14 score as canonical metric with PREREG.yaml; no standalone `METRIC.md` or machine-readable canonical metric declaration |
| 12 | `EXPERIMENT_LOG.jsonl` or similar append-only ledger | PARTIAL | `findings/*/leakage-flags.jsonl` and `corpus/medical-guidelines/chunks.jsonl` exist; no top-level `EXPERIMENT_LOG.jsonl` or run-indexed append-only ledger |
| 13 | Eval/judge code locked from agent-edit (per CLAUDE.md or PROGRAM.md) | PARTIAL | CLAUDE.md §7 freezes `data/seed_kg/` and `corpus/pins/`; `scripts/hooks/` guards secrets/prod-URLs; no explicit lock on `scripts/sovereign_bench.py` or judge scripts against agent mutation |

---

## Top-3 Recommended Next Actions

1. **Create `.claude/` directory with `settings.json` (hooks), `skills/`, and `agents/`** — Items 2, 3, and 4 all fail because this directory is absent. Adding UserPromptSubmit/PreToolUse hooks in `settings.json` closes the guardrail gap that CLAUDE.md §0 currently handles only by convention.

2. **Add `findings/PROGRAM.md`** with `allow_paths`, `lock_paths`, canonical metric declaration, and autoresearch loop charter — Item 10 fails outright; Items 11 and 13 are only PARTIAL because this file is missing. A single PROGRAM.md would close all three.

3. **Create `EXPERIMENT_LOG.jsonl`** as an append-only ledger at repo root (or `results/`) with one JSON object per eval run (run_id, timestamp, model, metric_value, decision) — Item 12 is PARTIAL; this is the lowest-friction change and directly enables the autoresearch loop's convergence tracking.
