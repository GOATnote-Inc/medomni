# Hourly Best-Practices Audit — 2026-05-09T12:13Z

## PART 1 — Public Surface Health

| URL | Expected | Got | Status |
|-----|----------|-----|--------|
| `https://www.thegoatnote.com/4UWHAt/` | 308 or 200 | **403** | FAIL |
| `https://www.thegoatnote.com/` | 200 | **403** | FAIL |

**⚠ Both public endpoints returning 403 — surface may be down or access-restricted.**

## PART 2 — Best-Practices Checklist

| # | Check | Result | Evidence |
|---|-------|--------|----------|
| 1 | CLAUDE.md with §0 hot-path markers | **PASS** | `/CLAUDE.md` exists; §0 RunPod PTY-echo warning present |
| 2 | `.claude/skills/` procedural-knowledge skills | **FAIL** | `.claude/` directory does not exist |
| 3 | `.claude/agents/` subagent definitions | **FAIL** | `.claude/` directory does not exist |
| 4 | `.claude/settings.json` with hooks (UserPromptSubmit, PreToolUse) | **FAIL** | `.claude/` directory does not exist; hooks live in `scripts/hooks/` only |
| 5 | `.mcp.json` for external tools | **FAIL** | File not present; no MCP tool wiring found |
| 6 | `tests/` with discoverable pytest tests | **PASS** | 8 test files found (`test_healthbench_judge.py`, `test_triton_judge.py`, etc.) |
| 7 | `scripts/` automation (no ask-user-to-run pattern) | **PASS** | 50+ scripts present; fully automated (`preflight.sh`, `health_check_all_pods.sh`, etc.) |
| 8 | No `cat .env` in `scripts/` | **PASS** | `grep -rE 'cat \.env'` returns clean |
| 9 | `.pre-commit-config.yaml` safety hooks | **PASS** | `.pre-commit-config.yaml` exists; `scripts/hooks/no_cloud_llm_keys.sh` + `no_prod_url_leak.sh` present |
| 10 | `PROGRAM.md` charter with `allow_paths`/`lock_paths` | **FAIL** | No `PROGRAM.md` anywhere; `findings/2026-04-29-reproducibility/design.md` references immutable eval files but lacks formal allow/lock path declarations |
| 11 | Single canonical metric defined | **PARTIAL** | `results/canonical-2026-05-01-hb-hard-n1000/CARD.md` defines HealthBench Hard mean score (gpt-4.1) as the ship-rule metric, but no single machine-readable `canonical_metric.yaml` or env var — discovered by convention only |
| 12 | `EXPERIMENT_LOG.jsonl` append-only ledger | **FAIL** | Not found anywhere in repo tree |
| 13 | Eval/judge code locked from agent-edit | **PARTIAL** | `findings/2026-04-29-reproducibility/design.md:86` calls out "immutable `eval/prepare.py`"; CLAUDE.md §7 freezes `corpus/pins/` and `data/seed_kg/`; no machine-enforceable `lock_paths` list covers eval harness scripts |

**Summary: 4 PASS / 5 FAIL / 2 PARTIAL / 2 FAIL (public surface)**

## PART 3 — Top-3 Recommended Next Actions

1. **Create `.claude/` directory with `settings.json` hooks** (items 2–4): Add `UserPromptSubmit` and `PreToolUse` hooks that call `scripts/hooks/no_cloud_llm_keys.sh` and `no_prod_url_leak.sh`. Add `.claude/skills/` with at least a `medical-eval.md` procedural skill and `.claude/agents/` with a `judge-agent.md` subagent definition. This is the single highest-leverage gap — it's what converts the scripts-based guardrails into Claude Code-native guardrails.

2. **Create `PROGRAM.md` at repo root with `allow_paths`/`lock_paths`** (item 10): Formalise the autoresearch charter with machine-readable path locks (e.g. `lock_paths: [scripts/healthbench_runner.py, corpus/pins/, data/seed_kg/]` and `allow_paths: [findings/, results/]`). This closes the Karpathy-autoresearch contract and gives future agents explicit edit-scope boundaries.

3. **Add `EXPERIMENT_LOG.jsonl` and a `canonical_metric.yaml`** (items 11–12): Create `results/EXPERIMENT_LOG.jsonl` (append-only; one JSON line per eval run with `run_id`, `metric`, `value`, `timestamp`) and `results/canonical_metric.yaml` (single source of truth: `metric: healthbench_hard_mean_score`, `grader: gpt-4.1-2025-04-14`, `ship_threshold: +0.020`). This makes the RL/eval loop auditable and prevents metric drift across sessions.
