# Hourly Best-Practices Audit — 2026-05-09T15:13:38Z

## PART 1 — Public Surface Health

| URL | Expected | Got | Status |
|-----|----------|-----|--------|
| `https://www.thegoatnote.com/4UWHAt/` | 308 or 200 | **403** | FAIL |
| `https://www.thegoatnote.com/` | 200 | **403** | FAIL |

**ALERT: Both public URLs returned 403. Investigate DNS/CDN/Vercel config.**

---

## PART 2 — Best-Practices Checklist

### Claude Code Hygiene

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| 1 | `CLAUDE.md` exists with §0 hot-path markers | **PASS** | `CLAUDE.md` present; `grep '§0'` returns 1 match |
| 2 | `.claude/skills/` has procedural-knowledge skills | **FAIL** | No `.claude/` directory exists in repo root |
| 3 | `.claude/agents/` has subagent definitions | **FAIL** | No `.claude/` directory exists in repo root |
| 4 | `.claude/settings.json` has hooks (UserPromptSubmit, PreToolUse, etc.) | **FAIL** | No `.claude/settings.json`; guardrail hooks not wired |
| 5 | `.mcp.json` present (if external tools used) | **FAIL** | File absent; MCP tool surface undefined |

### Boris Cherny 'no-terminal' Pattern

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| 6 | `tests/` exists with pytest-discoverable tests | **PARTIAL** | 8 `test_*.py` files exist; `pytest` module not installed in env (`No module named pytest`) |
| 7 | `scripts/` has automation scripts (no ask-user pattern) | **PASS** | 52 scripts present covering build, serve, bench, eval, health-check |
| 8 | `.env` never `cat`-ed in scripts (grep clean) | **PASS** | `grep -rE 'cat \.env'` in `scripts/` returns 0 matches |
| 9 | Pre-commit hooks for safety (`.pre-commit-config.yaml`) | **PASS** | Config present with `detect-private-key`, ruff, trailing-whitespace, large-file guard |

### Karpathy Autoresearch Pattern

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| 10 | `PROGRAM.md` (or charter) under `findings/` with `allow_paths`/`lock_paths` | **FAIL** | No `PROGRAM.md` or equivalent charter file found anywhere in repo |
| 11 | Single canonical metric defined for active eval/RL loop | **PARTIAL** | `CARD.md` references "HealthBench Hard accuracy" as primary metric; no machine-readable `canonical_metric` constant or dedicated config file |
| 12 | `EXPERIMENT_LOG.jsonl` or append-only ledger | **FAIL** | No experiment log found; only `leakage-flags.jsonl` (domain-specific) and corpus `chunks.jsonl` exist |
| 13 | Eval/judge code locked from agent-edit | **PARTIAL** | `CLAUDE.md §7` documents frozen paths (`data/seed_kg/`, `corpus/pins/`, `findings/research/...freeze-cert*`); no programmatic lock via `PROGRAM.md` `lock_paths` |

---

## PART 3 — Top-3 Highest-Leverage Missing Items

1. **`.claude/` directory scaffold** (items 2, 3, 4): Create `.claude/settings.json` with `UserPromptSubmit` + `PreToolUse` hooks mirroring the secret-grep guard already in `scripts/_runpod_ssh.sh`; add `.claude/skills/` and `.claude/agents/` stubs. Single highest-leverage action for session safety and autonomous capability.

2. **`findings/PROGRAM.md` autoresearch charter** (items 10, 13): Define `allow_paths`, `lock_paths`, the canonical metric (`hb_hard_accuracy`), and the loop entry-point. Converts the ad-hoc §7 freeze into a machine-readable contract that agents can verify.

3. **`EXPERIMENT_LOG.jsonl` append-only ledger** (item 12): A single JSONL file tracking `{timestamp, run_id, metric, value, notes}` per eval run, written by `write_card.py`. Enables trend analysis and guards against silent regression — current state has isolated `CARD.md` files with no cross-run aggregation.

---

*Generated by hourly audit loop · medomni · 2026-05-09T15:13:38Z*
