# Auto Audit — 2026-05-09 16:00 UTC

Generated: 2026-05-09T16:14:01Z

## Part 1 — Public Surface Health

| URL | Expected | Got | Status |
|-----|----------|-----|--------|
| `https://www.thegoatnote.com/4UWHAt/` | 308 or 200 | **403** | FAIL |
| `https://www.thegoatnote.com/` | 200 | **403** | FAIL |

**Both endpoints returning 403 — investigate Vercel/CDN config immediately.**

## Part 2 — Best-Practices Audit

| # | Item | Result | Evidence |
|---|------|--------|----------|
| 1 | CLAUDE.md in repo root with §0 hot-path markers | **PASS** | `CLAUDE.md` present; §0 section confirmed with RunPod PTY warning and incident history |
| 2 | `.claude/skills/` with procedural-knowledge skills | **FAIL** | No `.claude/` directory exists in repo |
| 3 | `.claude/agents/` with subagent definitions | **FAIL** | No `.claude/` directory exists in repo |
| 4 | `.claude/settings.json` with hooks (UserPromptSubmit, PreToolUse, etc.) | **FAIL** | No `.claude/` directory exists in repo |
| 5 | `.mcp.json` for external tool config | **PARTIAL** | No `.mcp.json` present; MCP tools (GitHub, Asana, Gmail, Drive) active in session but not repo-committed |
| 6 | `tests/` with discoverable pytest tests | **PASS** | 8 `test_*.py` files in `tests/` (triton judge, healthbench grader, ship rule, clinical demo, etc.) |
| 7 | `scripts/` with automation (no manual-run-command pattern) | **PASS** | 50+ scripts covering factory loop, serve, bench, health-check, CI hooks |
| 8 | `.env` handling never `cat`s values in `scripts/` | **PASS** | `grep -rE 'cat \.env'` returns 0 matches; `_runpod_ssh.sh` uses `awk -F=` pattern per CLAUDE.md §0 |
| 9 | Pre-commit hooks for safety | **PASS** | `.pre-commit-config.yaml` with `no-prod-url-leak` + `no-cloud-llm-keys` local hooks + `detect-private-key` + ruff |
| 10 | `PROGRAM.md`/charter under `findings/` with allow_paths/lock_paths | **PARTIAL** | Charter lives in `LOOP-STATUS.md` (root) not `findings/PROGRAM.md`; no machine-readable `allow_paths`/`lock_paths` fields |
| 11 | Single canonical metric defined for active eval/RL loop | **PARTIAL** | `LOOP-STATUS.md` references "V_{n+1} beating V_n by ≥5%" but metric name (e.g. `healthbench_score`) not declared in one authoritative place |
| 12 | `EXPERIMENT_LOG.jsonl` or append-only ledger | **FAIL** | No global `EXPERIMENT_LOG.jsonl`; only per-finding `leakage-flags.jsonl`; `results/` dirs exist but no append-only structured ledger |
| 13 | Eval/judge code locked from agent-edit | **PARTIAL** | `CLAUDE.md §7` freezes `data/seed_kg/`, `corpus/pins/`, `findings/research/`; judge scripts in `scripts/` not explicitly listed as locked; no `lock_paths` in `PROGRAM.md` |

## Top 3 Recommended Next Actions

1. **Create `.claude/` with `settings.json`, `skills/`, `agents/`** — fixes items 2, 3, 4 in one PR. Add `UserPromptSubmit` hook wired to `scripts/hooks/no_prod_url_leak.sh` and a `PreToolUse` hook for `Bash` blocking `cat .env`. Add at minimum one skill (e.g. `autoresearch-loop.md`) and one subagent def (e.g. `judge-probe.md`).

2. **Create `findings/PROGRAM.md`** with `allow_paths`, `lock_paths` (explicitly listing `scripts/serve_judge_*.sh`, `mla/judge/`, `tests/`), and `canonical_metric: healthbench_hard_1000_score` — fixes items 10, 11, 13 in one file.

3. **Create `EXPERIMENT_LOG.jsonl`** as a repo-root append-only JSONL (one entry per training run: `{timestamp, version, canonical_metric, score, delta_vs_prev, card_path}`) and wire `scripts/write_card.py` to append on CARD write — fixes item 12 and provides the audit trail Karpathy pattern requires.
