# Hourly Best-Practices Audit — 2026-05-09T10:13:22Z

## PART 1 — Public Surface Health

| URL | Expected | Got | Status |
|---|---|---|---|
| `https://www.thegoatnote.com/4UWHAt/` | 308 or 200 | **403** | ❌ FAIL |
| `https://www.thegoatnote.com/` | 200 | **403** | ❌ FAIL |

**Both public endpoints returning 403. Requires immediate investigation.**

---

## PART 2 — Best-Practices Checklist

### Claude Code (Anthropic)

| # | Item | Status | Evidence |
|---|---|---|---|
| 1 | CLAUDE.md with §0 hot-path markers | **PASS** | `CLAUDE.md` present at repo root; `§0 — HOT-PATH MARKERS` section confirmed |
| 2 | `.claude/skills/` with procedural-knowledge skills | **FAIL** | `.claude/` directory does not exist in repo |
| 3 | `.claude/agents/` with subagent definitions | **FAIL** | `.claude/` directory does not exist in repo |
| 4 | `.claude/settings.json` with hooks (UserPromptSubmit, PreToolUse, etc.) | **FAIL** | `.claude/` directory does not exist; no hooks wired |
| 5 | `.mcp.json` for external tool declarations | **FAIL** | File not found at repo root |
| 6 | `tests/` with discoverable pytest tests | **PASS** | 8 test files found: `test_healthbench_judge.py`, `test_triton_judge.py`, `test_ship_rule_eval.py`, etc. |

### Boris Cherny 'no terminal' pattern

| # | Item | Status | Evidence |
|---|---|---|---|
| 7 | `scripts/` has automation scripts | **PASS** | 50+ scripts present: `healthbench_runner.py`, `bench_judge_throughput.py`, `preflight.sh`, etc. |
| 8 | `.env` handling never uses `cat .env` | **PASS** | `grep -rE 'cat \.env\|cat .env' scripts/` returns zero matches |
| 9 | Pre-commit hooks configured | **PASS** | `.pre-commit-config.yaml` present with ruff, detect-private-key, and `no-prod-url-leak` local hook |

### Karpathy autoresearch

| # | Item | Status | Evidence |
|---|---|---|---|
| 10 | `PROGRAM.md` (or charter) under `findings/` with `allow_paths`/`lock_paths` | **FAIL** | No `PROGRAM.md` found; 39 finding subdirs exist, each with `PREREG.yaml`, but no top-level autoresearch charter |
| 11 | Single canonical metric defined for active eval/RL loop | **PARTIAL** | `results/canonical-2026-05-01-hb-hard-n1000/CARD.md` defines `mean score (gpt-4.1)` as canonical metric with ship rule (+0.020 CI>0), but not declared in a single machine-readable registry file |
| 12 | `EXPERIMENT_LOG.jsonl` or append-only ledger | **FAIL** | No top-level ledger found; only per-finding `leakage-flags.jsonl` files and `LOOP-STATUS.md` (markdown, not JSONL) |
| 13 | Eval/judge code locked from agent-edit | **PARTIAL** | `CLAUDE.md §7` freezes `data/seed_kg/` and `corpus/pins/`; `PREREG.yaml` files declare frozen reference models; but no explicit `lock_paths` list covering `scripts/grade_hb_parallel.py`, `mla/`, or judge-serving scripts |

---

## PART 3 — Top-3 Recommended Next Actions

1. **Create `.claude/` directory with `settings.json` (hooks), `skills/`, and `agents/`** — Items 2, 3, 4 are all FAIL and share the same root cause: `.claude/` is absent. A single `mkdir -p .claude/{skills,agents}` + `settings.json` with `UserPromptSubmit`/`PreToolUse` hooks (at minimum a secret-grep guard mirroring the `_runpod_ssh.sh` guard) would flip three FAILs to PASS at once.

2. **Add `findings/PROGRAM.md` with `allow_paths`, `lock_paths`, and the canonical metric** — Item 10 FAIL + items 11/12 PARTIAL share this gap. A PROGRAM.md declaring `primary_metric: hb_hard_mean_score_gpt41`, `lock_paths: [scripts/grade_hb_parallel.py, mla/, corpus/pins/]`, and `allow_paths: [findings/, results/]` would satisfy items 10, 11 (machine-readable), and 13 in one file.

3. **Create `EXPERIMENT_LOG.jsonl` as the append-only run ledger** — Item 12 FAIL. Each eval run (already producing a CARD.md) should also append one JSON line `{run_id, timestamp, metric, value, ci_lo, ci_hi, ship_decision}` to a root-level `EXPERIMENT_LOG.jsonl`. This enables programmatic trend analysis without parsing markdown.

---

*Audit generated by Claude Code at 2026-05-09T10:13:22Z. Read-only audit — no non-audit code modified.*
