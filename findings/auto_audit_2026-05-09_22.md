# Hourly Best-Practices Audit — 2026-05-09T22:13Z

## PART 1 — Public Surface Health

| URL | Expected | Got | Status |
|-----|----------|-----|--------|
| `https://www.thegoatnote.com/4UWHAt/` | 308 or 200 | **403** | FAIL |
| `https://www.thegoatnote.com/` | 200 | **403** | FAIL |

> Both endpoints returning 403. May indicate Vercel/CDN access policy change or deployment issue.

---

## PART 2 — Best-Practices Checklist

### Claude Code Patterns

| # | Item | Result | Evidence |
|---|------|--------|----------|
| 1 | CLAUDE.md with §0 hot-path markers | **PASS** | `/CLAUDE.md` exists; `§0` marker confirmed, 8 isolation rules present |
| 2 | `.claude/skills/` procedural-knowledge skills | **FAIL** | `.claude/` directory does not exist at repo root |
| 3 | `.claude/agents/` subagent definitions | **FAIL** | `.claude/` directory does not exist at repo root |
| 4 | `.claude/settings.json` with hooks (UserPromptSubmit, PreToolUse, etc.) | **FAIL** | `.claude/` directory does not exist; no automated guardrail hooks wired |
| 5 | `.mcp.json` (external tool declarations) | **FAIL** | File not present; no MCP server config declared |
| 6 | `tests/` with discoverable pytest tests | **PASS** | 8 test files found: `test_clinical_case.py`, `test_healthbench_judge.py`, `test_triton_judge.py`, etc. |

### Boris Cherny 'no terminal' Patterns

| # | Item | Result | Evidence |
|---|------|--------|----------|
| 7 | `scripts/` has automation scripts | **PASS** | 10+ scripts: `_runpod_ssh.sh`, `adversarial_probe.py`, `bench_judge_throughput.py`, etc. |
| 8 | No `cat .env` in scripts (secret-echo guard) | **PASS** | `grep -rE 'cat \.env'` in `scripts/` returns zero matches |
| 9 | Pre-commit hooks for safety | **PASS** | `.pre-commit-config.yaml` present; hooks: `no-prod-url-leak`, `no-cloud-llm-keys`, `ruff`, `detect-private-key` |

### Karpathy Autoresearch Patterns

| # | Item | Result | Evidence |
|---|------|--------|----------|
| 10 | `PROGRAM.md` with `allow_paths`/`lock_paths` | **FAIL** | No `PROGRAM.md` found under `findings/`; no `allow_paths`/`lock_paths` declarations anywhere |
| 11 | Single canonical metric defined | **PARTIAL** | `results/canonical-2026-05-01-hb-hard-n1000/CARD.md` defines canonical HealthBench Hard benchmark, but no machine-readable single-metric variable or `PROGRAM.md` to lock it |
| 12 | `EXPERIMENT_LOG.jsonl` append-only ledger | **FAIL** | No experiment log file found anywhere in repo; no `.jsonl` ledger in `results/` |
| 13 | Eval/judge code locked from agent-edit | **PARTIAL** | CLAUDE.md §7 freezes `corpus/pins/healthbench-hard-1000.yaml`; pre-commit blocks prod URL/key leaks; but no explicit lock on eval scripts (`scripts/healthbench_runner.py`, judge code) via `PROGRAM.md` or file-level guard |

---

## Top-3 Highest-Leverage Missing Items

1. **Create `.claude/` directory with `settings.json` hooks** (items 2–4): The entire Claude Code automation layer is absent. Adding `UserPromptSubmit` + `PreToolUse` hooks in `.claude/settings.json` and populating `.claude/skills/` would close three FAILs at once and enable automated guardrails beyond pre-commit.

2. **Add `EXPERIMENT_LOG.jsonl` append-only ledger** (item 12): No traceability for autoresearch runs. A simple append-only JSONL at `results/EXPERIMENT_LOG.jsonl` (one record per eval: timestamp, run_id, metric, score) would satisfy the Karpathy autoresearch pattern and make regressions detectable.

3. **Create `findings/PROGRAM.md` with `allow_paths`/`lock_paths`** (item 10): Without a machine-readable charter, agent-edit scope is enforced only by prose in CLAUDE.md. A `PROGRAM.md` with explicit `lock_paths: [scripts/healthbench_runner.py, corpus/pins/]` and `allow_paths: [results/, findings/]` would close item 10 and resolve the PARTIAL on item 13.

---

*Generated by hourly audit loop · 2026-05-09T22:13Z*
