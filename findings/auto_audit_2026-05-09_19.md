# Hourly Best-Practices Audit — 2026-05-09T19:13:38Z

## PART 1 — Public Surface Health

| URL | Expected | Got | Status |
|-----|----------|-----|--------|
| `https://www.thegoatnote.com/4UWHAt/` | 308 or 200 | **403** | ❌ FAIL |
| `https://www.thegoatnote.com/` | 200 | **403** | ❌ FAIL |

> Both endpoints returned 403. May indicate Vercel auth wall, CDN block, or misconfigured redirect. Investigate origin config.

---

## PART 2 — Best-Practices Checklist

### Claude Code

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | `CLAUDE.md` in repo root with §0 hot-path markers | **PASS** | `CLAUDE.md` present; §0 HOT-PATH MARKERS section exists with RunPod PTY-echo warning |
| 2 | `.claude/skills/` with procedural-knowledge skills | **FAIL** | Directory `.claude/` does not exist |
| 3 | `.claude/agents/` with subagent definitions | **FAIL** | Directory `.claude/` does not exist |
| 4 | `.claude/settings.json` with hooks (UserPromptSubmit, PreToolUse, etc.) | **FAIL** | `.claude/settings.json` absent; `scripts/hooks/` has `no_cloud_llm_keys.sh` + `no_prod_url_leak.sh` but not wired as Claude Code hooks |
| 5 | `.mcp.json` for external MCP tools | **FAIL** | File not present; no MCP server config found |
| 6 | `tests/` with pytest-discoverable tests | **PASS** | 8 test files found: `test_clinical_case.py`, `test_healthbench_grader_wire.py`, `test_triton_judge.py`, etc. |

### Boris Cherny 'no terminal' pattern

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 7 | `scripts/` has automation scripts (no ask-user-to-run-commands pattern) | **PASS** | 40+ scripts present including `preflight.sh`, `run_demo.sh`, `health_check_all_pods.sh` |
| 8 | `.env` handling never uses `cat .env` | **PASS** | `grep -rE 'cat \.env\|cat .env' scripts/` → zero matches |
| 9 | Pre-commit hooks config (`.pre-commit-config.yaml`) | **PASS** | `.pre-commit-config.yaml` present at repo root |

### Karpathy autoresearch

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 10 | `PROGRAM.md` (or charter) under `findings/` with allow_paths/lock_paths | **FAIL** | No `PROGRAM.md` found; `findings/` has `INDEX.md` and experiment CARDs but no autoresearch charter |
| 11 | Single canonical metric defined for active eval/RL loop | **PARTIAL** | `adversarial_probe.py` uses `pass_rate`; PREREG.yaml files exist per experiment but no repo-wide `canonical_metric` declaration |
| 12 | `EXPERIMENT_LOG.jsonl` or append-only ledger | **FAIL** | No `EXPERIMENT_LOG.jsonl` found; only per-experiment `leakage-flags.jsonl` files |
| 13 | Eval/judge code locked from agent-edit | **PARTIAL** | `CLAUDE.md §1` and `§7` list frozen paths; PREREG.yamls note "frozen reference model"; no machine-readable `lock_paths` list protecting eval scripts themselves |

---

## PART 3 — Top-3 Recommended Next Actions

1. **Create `.claude/settings.json` with UserPromptSubmit + PreToolUse hooks** (items 2–4): Wire existing `scripts/hooks/no_cloud_llm_keys.sh` and `no_prod_url_leak.sh` as Claude Code hooks; add `.claude/skills/` and `.claude/agents/` directories with at least stub files. This closes 3 FAIL items in one PR.

2. **Add `findings/PROGRAM.md` autoresearch charter with `lock_paths` + `canonical_metric`** (items 10–13): Define the single primary metric (e.g. `adversarial_pass_rate ≥ 0.95`), list locked eval scripts (`scripts/ship_rule_eval.py`, `scripts/adversarial_probe.py`, etc.), and add an `EXPERIMENT_LOG.jsonl` append target for the RL loop. Closes 3 FAIL/PARTIAL items.

3. **Investigate 403 on `www.thegoatnote.com`** (PART 1): Both root and `/4UWHAt/` redirect return 403 — check Vercel authentication settings, CDN firewall rules, or whether the deployment is paused. This is the highest-urgency operational issue.

---

*Generated by hourly audit loop — read-only, no code modified.*
