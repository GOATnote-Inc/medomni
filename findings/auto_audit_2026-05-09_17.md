# Hourly Best-Practices Audit — 2026-05-09T17:14Z

## PART 1 — Public Surface Health

| URL | Expected | Got | Status |
|-----|----------|-----|--------|
| https://www.thegoatnote.com/4UWHAt/ | 308 or 200 | **403** | ⚠ FAIL |
| https://www.thegoatnote.com/ | 200 | **403** | ⚠ FAIL |

**Both endpoints returning 403. Demo surface is down.**

---

## PART 2 — Best-Practices Checklist

| # | Item | Result | Evidence |
|---|------|--------|----------|
| 1 | CLAUDE.md in repo root with §0 hot-path markers | **PASS** | `/home/user/medomni/CLAUDE.md` exists; §0 present with RunPod PTY warning + secret-grep guard |
| 2 | `.claude/skills/` with procedural-knowledge skills | **FAIL** | `.claude/` directory does not exist |
| 3 | `.claude/agents/` with subagent definitions | **FAIL** | `.claude/` directory does not exist |
| 4 | `.claude/settings.json` with hooks (UserPromptSubmit, PreToolUse, etc.) | **FAIL** | `.claude/settings.json` does not exist; no programmatic guardrail hooks wired |
| 5 | `.mcp.json` (if external tools used) | **PARTIAL** | No `.mcp.json`; stack is sovereign (no external LLM APIs), so MCP may be intentionally absent — ambiguous whether any external tool integrations need declaring |
| 6 | `tests/` with pytest-discoverable tests | **PASS** | 8 test files found: `test_clinical_case.py`, `test_healthbench_judge.py`, `test_ship_rule_eval.py`, etc. |
| 7 | `scripts/` automation (no ask-user-to-run pattern) | **PASS** | 40+ scripts present covering serve, bench, build, health-check, ssh tunnel automation |
| 8 | `.env` handling never cats values in scripts | **PASS** | `grep -rE 'cat \.env' scripts/` → 0 hits; `_runpod_ssh.sh` uses `awk -F=` pattern |
| 9 | `.pre-commit-config.yaml` with safety hooks | **PASS** | Pre-commit exists with `detect-private-key`, `no-prod-url-leak` local hook, ruff linter |
| 10 | `PROGRAM.md` (or similar) under `findings/` with `allow_paths`/`lock_paths` | **FAIL** | No `PROGRAM.md` with Karpathy-style formal `allow_paths`/`lock_paths`; `LOOP-STATUS.md` has a write-boundary table but uses prose, not machine-checkable syntax |
| 11 | Single canonical metric defined for active eval/RL loop | **PARTIAL** | HealthBench Hard gpt-4.1 score is the de-facto canonical metric per `results/canonical-2026-05-01-hb-hard-n1000/CARD.md` ship rule (Δ ≥ +0.020, 95% CI excl. 0), but not declared in a single top-level charter file — discoverable only by reading CARD.md |
| 12 | `EXPERIMENT_LOG.jsonl` or append-only ledger | **FAIL** | No experiment log found; only `leakage-flags.jsonl` under individual eval findings; iteration history lives in `LOOP-STATUS.md` (markdown prose, not machine-parseable JSONL) |
| 13 | Eval/judge code locked from agent-edit (CLAUDE.md or PROGRAM.md) | **PARTIAL** | CLAUDE.md §7 locks `data/seed_kg/` and `corpus/pins/`; reproducibility brief mentions "Immutable `eval/prepare.py`"; `LOOP-STATUS.md` hard-rules list `mla/judges/*` as training-loop-only write surface — but no explicit agent-edit prohibition on eval scripts in CLAUDE.md itself |

---

## Top-3 Recommended Next Actions

1. **[URGENT] Investigate 403 on both public URLs** — `https://www.thegoatnote.com/` and `https://www.thegoatnote.com/4UWHAt/` both returning 403; demo surface is fully down. Check Vercel deployment status (without touching `.vercel/` config per isolation contract) and surface to user immediately.

2. **Create `.claude/` directory with `settings.json` hooks** — Items 2, 3, 4 all fail because `.claude/` is absent. Adding `PreToolUse` + `PostToolUse` hooks for the secret-grep guard (currently only in `_runpod_ssh.sh`) and a `UserPromptSubmit` hook for the isolation contract would close 3 checklist items at once and move guardrails from documentation to enforcement.

3. **Add `PROGRAM.md` with machine-readable `allow_paths`/`lock_paths` + `EXPERIMENT_LOG.jsonl`** — Items 10 and 12 fail. A single `findings/PROGRAM.md` declaring canonical metric (`healthbench_hard_gpt41_mean_score`), locked paths (`mla/judges/`, `corpus/pins/`, `data/seed_kg/`), and allowed write surfaces (matching `LOOP-STATUS.md` write-boundary table) would close item 10. Replacing per-iter markdown heartbeats with a single append-only `EXPERIMENT_LOG.jsonl` would close item 12 and enable programmatic regression detection.
