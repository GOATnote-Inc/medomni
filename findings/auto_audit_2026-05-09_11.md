# Auto Audit — 2026-05-09 11:00 UTC

Generated: 2026-05-09T11:13:27Z  
Auditor: hourly-best-practices loop

---

## PART 1 — Public Surface Health

| URL | Expected | Got | Status |
|---|---|---|---|
| `https://www.thegoatnote.com/4UWHAt/` | 308 or 200 | **403** | **FAIL** |
| `https://www.thegoatnote.com/` | 200 | **403** | **FAIL** |

> Both URLs returning 403. Deploy surface may be down or Vercel access-restricted. Investigate immediately — isolation contract (§1 rule 2) forbids touching Vercel directly; escalate to user.

---

## PART 2 — Best-Practices Checklist

| # | Item | Status | Evidence |
|---|---|---|---|
| 1 | CLAUDE.md with §0 hot-path markers | **PASS** | `CLAUDE.md` present at repo root; §0 "HOT-PATH MARKERS" section with RunPod PTY-echo surface warning |
| 2 | `.claude/skills/` procedural-knowledge skills | **FAIL** | No `.claude/` directory exists |
| 3 | `.claude/agents/` subagent definitions | **FAIL** | No `.claude/` directory exists |
| 4 | `.claude/settings.json` with hooks (PreToolUse, etc.) | **FAIL** | No `.claude/settings.json` found |
| 5 | `.mcp.json` for external tools | **PARTIAL** | No `.mcp.json`; repo is sovereign-local by design (no external LLM APIs), so absence may be intentional |
| 6 | `tests/` with pytest-discoverable tests | **PASS** | 8 test files: `test_clinical_case.py`, `test_healthbench_grader_wire.py`, `test_triton_judge.py`, etc. |
| 7 | `scripts/` automation (no ask-user-to-run pattern) | **PASS** | 50+ scripts: `factory_loop`, eval runners, `serve_*.sh`, `health_check_all_pods.sh` |
| 8 | `.env` handling never uses `cat .env` | **PASS** | `grep -rE 'cat \.env' scripts/` → 0 matches |
| 9 | Pre-commit hooks for safety | **PASS** | `.pre-commit-config.yaml` with `detect-private-key`, `no-prod-url-leak.sh`, `no_cloud_llm_keys.sh` hooks |
| 10 | `PROGRAM.md` under `findings/` with `allow_paths`/`lock_paths` | **FAIL** | No `PROGRAM.md`; `LOOP-STATUS.md` has a charter but lacks structured `allow_paths`/`lock_paths` per Karpathy autoresearch pattern |
| 11 | Single canonical metric for active eval/RL loop | **PARTIAL** | HealthBench Hard score is de-facto primary metric (per `results/*/CARD.md`), but no explicit `canonical_metric=` declaration in a config file |
| 12 | `EXPERIMENT_LOG.jsonl` append-only ledger | **PARTIAL** | `LOOP-STATUS.md` serves as append-only log but is Markdown; no machine-readable `EXPERIMENT_LOG.jsonl` found |
| 13 | Eval/judge code locked from agent-edit | **PARTIAL** | `CLAUDE.md §7` freezes `data/seed_kg/` and `corpus/pins/` but does not explicitly lock `mla/judges/` or eval runner scripts |

---

## Top-3 Recommended Next Actions

1. **Create `.claude/` with `settings.json`, `skills/`, `agents/`** — Items 2, 3, 4 all FAIL because the `.claude/` directory is entirely absent. Adding `settings.json` with `PreToolUse`/`PostToolUse` hooks is the single highest-leverage Claude Code guardrail and unlocks the skills/agents pattern. Estimated effort: 30 min.

2. **Add `findings/PROGRAM.md`** with `allow_paths`, `lock_paths`, and `canonical_metric` declaration (items 10 + 11). This formalizes the Karpathy autoresearch isolation contract in a machine-readable way, enabling programmatic enforcement rather than prose-only CLAUDE.md §7. Estimated effort: 15 min.

3. **Create `EXPERIMENT_LOG.jsonl`** as an append-only JSONL ledger (item 12). Each CARD landing appends one JSON line with `{run_id, metric, score, date, model}`. Enables `jq`-based trend queries and replaces manual scanning of `results/*/CARD.md`. Estimated effort: 20 min + backfill of 3 existing CARDs.
