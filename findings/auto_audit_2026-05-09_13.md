# Hourly Best-Practices Audit — 2026-05-09T13:14Z

## PART 1 — Public Surface Health

| URL | Expected | Got | Status |
|-----|----------|-----|--------|
| `https://www.thegoatnote.com/4UWHAt/` | 308 or 200 | **403** | ❌ FAIL |
| `https://www.thegoatnote.com/` | 200 | **403** | ❌ FAIL |

> Both endpoints returned 403. Investigate Vercel/CDN firewall rules or geo-block.

---

## PART 2 — Best-Practices Checklist

### Claude Code best practices

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | CLAUDE.md in root with §0 hot-path markers | **PASS** | `CLAUDE.md` present; `## §0 — HOT-PATH MARKERS` at line 15 |
| 2 | `.claude/skills/` procedural-knowledge skills | **FAIL** | No `.claude/` directory exists in repo |
| 3 | `.claude/agents/` subagent definitions | **FAIL** | No `.claude/` directory exists in repo |
| 4 | `.claude/settings.json` with hooks (UserPromptSubmit, PreToolUse, etc.) | **FAIL** | No `.claude/` directory exists; no hooks configured |
| 5 | `.mcp.json` for external tool integrations | **FAIL** | File absent; no external MCP tool declarations |
| 6 | `tests/` with discoverable pytest tests | **PASS** | 8 `test_*.py` files found (`test_clinical_case.py`, `test_healthbench_judge.py`, etc.) |

### Boris Cherny 'no terminal' pattern

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 7 | `scripts/` automation scripts (no ask-user-to-run pattern) | **PASS** | 40+ scripts in `scripts/`; full automation coverage (bench, serve, build, eval) |
| 8 | `.env` handling never uses `cat .env` | **PASS** | `grep -rE 'cat \.env\|cat .env' scripts/` → 0 hits |
| 9 | Pre-commit hooks configured | **PASS** | `.pre-commit-config.yaml` present at repo root |

### Karpathy autoresearch pattern

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 10 | `PROGRAM.md` (or similar charter) under `findings/` with `allow_paths`/`lock_paths` | **FAIL** | No `PROGRAM.md` found anywhere; `findings/INDEX.md` exists but lacks path constraints |
| 11 | Single canonical metric defined for active eval/RL loop | **PARTIAL** | Multiple per-task metrics (`accuracy` in medqa/mmlu/pubmedqa, `axis:accuracy` in healthbench); no single top-level `PRIMARY_METRIC` constant declared |
| 12 | `EXPERIMENT_LOG.jsonl` or append-only ledger | **FAIL** | No `EXPERIMENT_LOG.jsonl` found; `results/*/CARD.md` files exist but are not append-only JSONL |
| 13 | Eval/judge code locked from agent-edit | **PARTIAL** | `CLAUDE.md §7` freezes `data/seed_kg/` and `corpus/pins/` but does not explicitly name `scripts/healthbench_runner.py` or judge scripts as agent-locked |

---

## PART 3 — Top-3 Highest-Leverage Next Actions

1. **Create `.claude/` with `settings.json` hooks** — Adds `UserPromptSubmit` and `PreToolUse` guardrail hooks; also enables `.claude/skills/` and `.claude/agents/` to be populated. Addresses items 2, 3, 4 in a single directory scaffold.

2. **Create `findings/PROGRAM.md`** — Define `allow_paths`, `lock_paths`, and a single `PRIMARY_METRIC` (recommend `healthbench_hard_accuracy` as the top-level canonical). Addresses items 10 and 11; gives the autoresearch loop a machine-readable charter.

3. **Create `EXPERIMENT_LOG.jsonl`** — Append-only ledger seeded from existing `results/*/CARD.md` data; entries should include `run_id`, `timestamp`, `metric`, `value`, `model_tag`. Addresses item 12 and enables automated trend tracking.

---

*Generated by Claude Code hourly audit loop · 2026-05-09T13:14Z*
