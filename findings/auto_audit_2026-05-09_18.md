# Hourly Best-Practices Audit — 2026-05-09T18:13Z

> Generated automatically. Read-only audit; no production code modified.

## PART 1 — Public Surface Health

| URL | Expected | Got | Status |
|-----|----------|-----|--------|
| `https://www.thegoatnote.com/4UWHAt/` | 308 or 200 | **403** | FAIL |
| `https://www.thegoatnote.com/` | 200 | **403** | FAIL |

**Both endpoints returned 403.** Possible causes: Vercel domain misconfiguration, firewall rule, or DNS propagation issue. Requires operator investigation.

---

## PART 2 — Best-Practices Checklist

### Claude Code patterns

1. **CLAUDE.md with §0 hot-path markers** — PASS · `CLAUDE.md` present at repo root; §0 HOT-PATH MARKERS section is the first section.

2. **.claude/skills/ with procedural skills** — FAIL · `.claude/skills/` directory does not exist.

3. **.claude/agents/ with subagent definitions** — FAIL · `.claude/agents/` directory does not exist.

4. **.claude/settings.json with hooks** — FAIL · `.claude/settings.json` does not exist; no `UserPromptSubmit`, `PreToolUse`, or `PostToolUse` hooks configured.

5. **.mcp.json (if external tools used)** — FAIL · `.mcp.json` not present; no external MCP tool manifest.

6. **tests/ with pytest-discoverable tests** — PASS · 8 test files found (`test_clinical_case.py`, `test_healthbench_judge.py`, `test_triton_judge.py`, etc.).

### Boris Cherny 'no terminal' patterns

7. **scripts/ with automation (no ask-user pattern)** — PASS · 20+ scripts present (`_runpod_ssh.sh`, `grade_hb_parallel.py`, `build_trtllm_engine.sh`, etc.).

8. **No `cat .env` in scripts/** — PASS · `grep -rE 'cat \.env'` returns 0 matches; `_runpod_ssh.sh` uses `awk -F=` pattern per §0.

9. **Pre-commit hooks (.pre-commit-config.yaml)** — PASS · `.pre-commit-config.yaml` present at repo root.

### Karpathy autoresearch patterns

10. **PROGRAM.md / charter with allow_paths / lock_paths** — FAIL · No `PROGRAM.md` or equivalent found under `findings/` or repo root. Per-experiment `PREREG.yaml` files exist but lack a global `allow_paths` / `lock_paths` agent-visible contract.

11. **Single canonical metric for active eval/RL loop** — PARTIAL · `findings/research/2026-04-29-medomni-v1-northstar/SPEC.md §6` defines pre-launch gates (HealthBench Hard ≥ 0.55, MedAgentBench ≥ 0.70, MedQA-USMLE ≥ 0.85). Per-run `PREREG.yaml` files name headline metrics. However, no single machine-readable canonical metric file (e.g. `METRIC.yaml`) is referenced by the eval harness.

12. **EXPERIMENT_LOG.jsonl (append-only run ledger)** — FAIL · No `EXPERIMENT_LOG.jsonl` found. Only `leakage-flags.jsonl` (per-eval artifact) and `corpus/medical-guidelines/chunks.jsonl` exist.

13. **Eval/judge code locked from agent edit** — PARTIAL · CLAUDE.md §7 lists frozen data paths (`data/seed_kg/`, `corpus/pins/`, `findings/research/2026-04-27-future-stack/`). Eval scripts (`scripts/grade_hb_parallel.py`, `scripts/healthbench_runner.py`, `mla/`) are not listed in a machine-readable lock manifest; relying on pre-commit prose-pattern guards only.

---

## Top-3 Recommended Next Actions

1. **Add `EXPERIMENT_LOG.jsonl`** (item 12 — FAIL) · An append-only ledger keyed by run ID is the minimum scaffold for the autoresearch loop to track cross-run regression. Create `findings/EXPERIMENT_LOG.jsonl` and wire `scripts/log_experiment.py` to append after each eval. Without it, every run is a write-once island.

2. **Add `.claude/settings.json` with guardrail hooks** (item 4 — FAIL) · `UserPromptSubmit` and `PreToolUse` hooks that enforce §0/§1 constraints at the harness level are the highest-leverage Claude Code safety layer — they run even when CLAUDE.md prose is not consulted. Add hooks that reject prompts referencing frozen paths and run the secret-grep guard.

3. **Write `findings/PROGRAM.md`** (item 10 — FAIL) · A single structured charter with `allow_paths`, `lock_paths`, and `eval_lock_paths` gives the autoresearch agent a machine-readable scope contract, preventing accidental edits to frozen eval code. PREREG files cover per-experiment scope but not the global agent permission boundary.

---

*Audit run at 2026-05-09T18:13:22Z · medomni HEAD · read-only*
