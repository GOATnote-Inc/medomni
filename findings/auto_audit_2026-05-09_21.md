# Hourly Best-Practices Audit — 2026-05-09T21:13:36Z

## PART 1 — Public Surface Health

| URL | Expected | Got | Status |
|-----|----------|-----|--------|
| `https://www.thegoatnote.com/4UWHAt/` | 308 or 200 | **403** | FAIL |
| `https://www.thegoatnote.com/` | 200 | **403** | FAIL |

> **ACTION REQUIRED:** Both public endpoints returning 403. Possible Vercel deployment issue or access-control misconfiguration (do NOT touch Vercel per §1 — surface to owner).

---

## PART 2 — Best-Practices Checklist

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | CLAUDE.md in repo root with §0 hot-path markers | **PASS** | `CLAUDE.md` exists; §0 present with RunPod PTY warning and durable mitigations |
| 2 | `.claude/skills/` has procedural-knowledge skills | **FAIL** | `.claude/` directory absent entirely |
| 3 | `.claude/agents/` has subagent definitions | **FAIL** | `.claude/` directory absent entirely |
| 4 | `.claude/settings.json` has hooks (UserPromptSubmit, PreToolUse, etc.) | **FAIL** | `.claude/` directory absent; guardrail hooks live only in `scripts/hooks/` (not Claude Code hooks) |
| 5 | `.mcp.json` present (if external tools used) | **PARTIAL** | File absent; acceptable if no external MCP tools active, but no evidence of explicit decision |
| 6 | `tests/` exists with pytest tests discoverable | **PASS** | 8 `test_*.py` files found: `test_clinical_case.py`, `test_healthbench_judge.py`, `test_triton_judge.py`, etc. |
| 7 | `scripts/` has automation scripts (no ask-user-to-run pattern) | **PASS** | 50+ automation scripts: `preflight.sh`, `healthbench_runner.py`, `ship_rule_eval.py`, etc. |
| 8 | `.env` handling never cats values (`cat .env` grep = 0) | **PASS** | `grep -rE 'cat \.env\|cat .env' scripts/` returns zero matches |
| 9 | Pre-commit hooks for safety (`.pre-commit-config.yaml`) | **PASS** | `.pre-commit-config.yaml` present; `scripts/hooks/` has `no_cloud_llm_keys.sh`, `no_prod_url_leak.sh` |
| 10 | `PROGRAM.md` (or charter) under `findings/` with allow/lock paths | **FAIL** | No `PROGRAM.md` found anywhere in repo; closest is CLAUDE.md §7 frozen paths |
| 11 | Single canonical metric defined for active eval/RL loop | **FAIL** | No `PRIMARY_METRIC` or `canonical_metric` definition found in `findings/` or `scripts/` |
| 12 | `EXPERIMENT_LOG.jsonl` or append-only eval ledger | **FAIL** | No `EXPERIMENT_LOG*` or `experiment_log*` found in repo |
| 13 | Eval/judge code locked from agent-edit | **PARTIAL** | CLAUDE.md §7 freezes `data/seed_kg/`, `corpus/pins/`, `findings/research/` but does not explicitly lock `scripts/healthbench_runner.py`, `scripts/ship_rule_eval.py`, or judge-serve scripts |

**Summary: 5 PASS / 5 FAIL / 3 PARTIAL**

---

## PART 3 — Top-3 Recommended Next Actions

1. **Create `.claude/` scaffold** (items 2, 3, 4 — highest leverage, 3 FAILs from one gap)
   - `mkdir -p .claude/{skills,agents}` + write `.claude/settings.json` with at minimum a `PreToolUse` hook wiring `scripts/hooks/no_cloud_llm_keys.sh` and `no_prod_url_leak.sh`. This converts the CLAUDE.md §1 isolation contract into enforced Claude Code guardrails.

2. **Add `PROGRAM.md` autoresearch charter** (items 10, 11, 13 — governance gap)
   - Define `allow_paths`, `lock_paths`, and one `PRIMARY_METRIC` (e.g., `healthbench_hard_1000_accuracy`). Reference `corpus/pins/healthbench-hard-1000.yaml` as the pin. Explicitly lock `scripts/healthbench_runner.py` and judge-serve scripts from agent edits.

3. **Initialize `EXPERIMENT_LOG.jsonl`** (item 12)
   - Single append-only file written by `scripts/write_card.py` and eval runners. One JSON object per run: `{ts, branch, metric, value, artifact_path}`. Enables automated regression detection across audit runs.
